# Extra autostart processes
# exec-once = uwsm app -- my-service

# Workaround: Hyprland v0.55 + NVIDIA clamps cursor at multi-monitor boundary
# on fresh login. A reload after the session settles rebuilds pointer bounds.
exec-once = sleep 2 && hyprctl reload
